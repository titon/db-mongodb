language: php

php:
  - 5.4
  - 5.5

services:
  - mongodb

before_script:
  #- mongo titon_test --eval 'db.addUser("mongo", "test");'
  - pecl upgrade mongo
  - pecl install mongoclient
  - composer selfupdate
  - composer install

notifications:
  email: false